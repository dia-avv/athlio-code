name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: athlio/package-lock.json

      - run: npm install --legacy-peer-deps
        working-directory: ./athlio

      # Not strictly needed on macOS, but leaving it won’t hurt.
      - run: npm rebuild rollup
        working-directory: ./athlio

      - run: npm run build
        working-directory: ./athlio

      - run: cp build/client/index.html build/client/404.html
        working-directory: ./athlio

      - uses: actions/upload-pages-artifact@v3
        with:
          path: athlio/build/client

  deploy:
    needs: build
    runs-on: macos-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
